kind: Pod
apiVersion: v1
metadata:
  name: env-test

spec:
  containers: 
  - name: nginx
    image: nginx
    env:
    - name: course
      value: 'kubernates'
    - name: duration
      value: 120 Hrs

#kubectl exec -it env-test -- bash [To login]